// 5. Write a program to print days of a week using switch case.

let x = 8;

switch (x) {
  case x < 8:
    console.log("Sunday");
    break;
  case 1:
    console.log(x);
    break;
  default:
    text = "No value found";
}